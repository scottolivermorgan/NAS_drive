# /opt/nextcloud/apache/zzz-nextcloud.conf

Timeout 3600
KeepAliveTimeout 3600

# Disable request body rate limiting (breaks chunked uploads)
<IfModule reqtimeout_module>
  RequestReadTimeout header=0 body=0
</IfModule>
