services:
  homepage:
    build:
      context: .
    container_name: Homepage
    restart: unless-stopped
    ports:
      - "1111:80"  # Map port 80 in the container to port 1111 on the host
