services:
  ampache:
    image: ampache/ampache:latest
    container_name: ampache
    restart: unless-stopped
    ports:
      - 8080:80
    volumes:
      - /mnt/ampache/config:/var/www/config
      - /mnt/ampache/log:/var/log/ampache
      - /mnt/ampache/mysql:/var/lib/mysql
      - /media/HD_1/Media/Music:/media